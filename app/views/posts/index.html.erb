<%= render "shared/partial" %>
<div class= "bg-success-subtle">
  <h2 class="text-center">投稿一覧</h2>
  <%= link_to "自分の投稿一覧", mypost_posts_path, class:"btn btn-primary m-2" %>
  <%= link_to "新規投稿", new_post_path, class:"btn btn-danger m-2" %>
  <div class="w-75 m-auto">
    <% @posts.each do |post| %>
    <div class="card m-3">
      <div class="card-body">
        <h5 class="card-title">カテゴリー</h5>
        <h6 class="card-subtitle mb-2 text-body-secondary"><%= post.category %></h6>
        <p class="card-text"><%= post.body %></p>
        <%= link_to "投稿内容閲覧", post, class:"btn btn-primary m-2" %>
      </div>
    </div>
    <% end %>
  </div>
</div>
